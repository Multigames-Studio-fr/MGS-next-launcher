<div id="waitingContainer" class="hidden fixed inset-0 z-50 flex items-center justify-center bg-black/70 p-6">
    <div id="waitingContent" class="w-full max-w-md bg-[#0b0b0b]/88 rounded-2xl p-6 md:p-8 flex flex-col items-center text-center shadow-2xl mx-auto">
        <!-- Spinner -->
        <div class="w-14 h-14 rounded-full border-4 border-white/10 border-t-[#F8BA59] animate-spin mb-4"></div>

        <!-- Texte d'attente (conserver ID) -->
        <div id="waitingTextContainer">
            <h2 class="text-lg md:text-xl font-semibold text-white mb-1"><%- lang('waiting.waitingText') %></h2>
            <p class="text-sm text-white/70">Veuillez patienter pendant la préparation...</p>
        </div>

        <!-- Optionnel: petit footer ou hint -->
        <div class="mt-4 text-xs text-white/50">Si cela prend trop de temps, vérifiez votre connexion ou relancez le launcher.</div>
    </div>
</div>